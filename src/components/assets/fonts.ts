import { Handlee, Poppins } from "next/font/google";

export const handlee = Handlee({ subsets: ["latin"], weight: "400" });
export const poppins = Poppins({ subsets: ["latin"], weight: ["400", "500"] });
